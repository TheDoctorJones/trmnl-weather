{%- comment -%} TEMPORARY: renders the debug probe from markup_shared. {%- endcomment -%}
<div class="layout" style="height: 100%; padding: var(--gap);">
  <span class="label label--small" style="word-break: break-all; line-height: 1.4;">{{ debug_probe }}</span>
</div>